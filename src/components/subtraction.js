import React from 'react';

const Subtracting = (props) => {

    return (
        <div>
            <button onClick={ props.onsubtracting }>minus</button>
        </div>
    )
};
export default Subtracting;